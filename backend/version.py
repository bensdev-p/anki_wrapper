"""The app's version. The desktop release workflow checks it matches the git tag (vX.Y.Z)."""

__version__ = "0.1.3"

# Where releases are published (the desktop app checks for updates here).
REPO = "bensdev-p/anki_wrapper"
